#include "main.h"
/**
* sub - a function that adds two integers and returns the result
* @one: first number input
* @two: second number input
* Return: one - two
*/
int sub(int one, int two)
{
return (one - two);
}
