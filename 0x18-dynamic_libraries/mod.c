#include "main.h"
/**
* mod - a function that adds two integers and returns the result
* @one: first number input
* @two: second number input
* Return: one % two
*/
int div(int one, int two)
{
return (one % two);
}
